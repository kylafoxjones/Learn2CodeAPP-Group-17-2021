import { ResetPasswordDto } from './reset-password-dto.model';

describe('ResetPasswordDto', () => {
  it('should create an instance', () => {
    expect(new ResetPasswordDto()).toBeTruthy();
  });
});
